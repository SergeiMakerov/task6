/**
 * package homeWork4;
 *
 * import java.util.LinkedList;
 *
 * public class Task2jtPrepoda {
 *     private static LinkedList<Integer> ll = new LinkedList<>();
 *
 *     public static void enqueue(int num) {// помещает элемент в конец очереди
 *         ll.addLast(num);
 *     }
 *
 *     public static int dequeue() { // возвращает первый элемент из очереди и удаляет его
 *         int num = ll.get(0);
 *         ll.remove(0);
 *         return num;
 *     }
 *
 *     public static int first() { // возвращает первый элемент из очереди, не удаляя
 *         int num = ll.get(0);
 *         return num;
 *     }
 * }
 */
